# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
node_modules/
dist/
dist-ssr/
*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build output
gcl5-app/dist/
gcl5-app/node_modules/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Temporary JSON snapshots (from CricHeroes API fetch)
*.json
!package.json
!package-lock.json
!tsconfig.json
!leaderboard.json
!matches_live.json
!matches_past.json
!matches_upcoming.json

# If you want to keep the snapshots, uncomment the lines above or remove the pattern.
# For now, I'll ignore everything but standard configs.
*.json
!package.json
!package-lock.json
!manifest.json
